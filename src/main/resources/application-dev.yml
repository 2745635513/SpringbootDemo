#server:
#  port: 8081
#  servlet:
#    context-path: /girl
#girl:
#  cupSize: C
#  age: 18
#content: "cupSize: ${cupSize}, age: ${age}}" 没有起到效果，不知道什么原因

spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://127.0.0.1:3306/test?serverTimezone=UTC
        username: root
        password: root
    jpa:
        hibernate:
          ddl-auto: update
          naming:
            physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        show-sql: true
